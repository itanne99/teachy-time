meta {
  name: Update User Profile
  type: http
  seq: 2
}

patch {
  url: {{BaseUrl}}/userProfile
  body: json
  auth: inherit
}

body:json {
  {
    "user_id": "{{user_id}}",
    "first_name": "Jane",
    "last_name": "Doe"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
